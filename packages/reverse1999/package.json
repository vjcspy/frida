{
  "name": "@packages/reverse1999",
  "version": "1.0.0",
  "main": "dist/index",
  "types": "src/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm run clean && pnpm run compile",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json -w",
    "lint": "eslint --fix --ext ts,tsx ."
  },
  "devDependencies": {
    "tsconfig": "workspace:*",
    "@packages/common": "workspace:*"
  },
  "dependencies": {
  }
}
