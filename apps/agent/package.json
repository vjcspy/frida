{
  "name": "agent",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "pnpm run clean && pnpm run compile",
    "clean": "rimraf -rf ./dist",
    "compile": "node build.js",
    "lint": "eslint src --ext .ts --fix"
  },
  "dependencies": {
    "@packages/common": "workspace:*",
    "@packages/mori": "workspace:*"
  }
}
